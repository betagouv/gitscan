services:
  postgresql:
    image: pgvector/pgvector:pg16
    container_name: csp_postgresql
    env_file:
      - env.d/postgresql
    environment:
      # Avoid a log error when starting the itou_postgres container:
      # > Role "root" does not exist.
      # Without this variable, the default Unix account ('root')
      # is used automatically when starting postgres.
      # https://www.postgresql.org/docs/current/libpq-envars.html
      - PGUSER=postgres
      - PGPASSWORD=password
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres:/docker-entrypoint-initdb.d
    healthcheck:
      test:
        - "CMD-SHELL"
        - "pg_isready"
        - "-d"
        - "$${POSTGRES_DB}"
      interval: 10s
      timeout: 5s
      retries: 5

  qdrant:
    image: qdrant/qdrant:latest
    restart: always
    container_name: csp_qdrant
    ports:
      - "6333:6333"
    expose:
      - 6333
    configs:
      - source: qdrant_config
        target: /qdrant/config/production.yaml
    volumes:
      - qdrant_data:/qdrant/storage

  redis:
    image: redis
    container_name: csp_redis
    restart: unless-stopped
    # settings/test.py alloue une base Redis par worker pytest-xdist ; 16 ne suffisent plus
    command: ["redis-server", "--databases", "64"]
    env_file:
      - env.d/redis
    ports:
      - "127.0.0.1:${REDIS_PORT:-6379}:6379"

  rustfs:
    image: rustfs/rustfs:latest
    container_name: csp_rustfs
    env_file:
      - env.d/rustfs
    environment:
      - RUSTFS_ADDRESS=0.0.0.0:9000
      - RUSTFS_CONSOLE_ADDRESS=0.0.0.0:9001
      - RUSTFS_CONSOLE_ENABLE=true
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - rustfs_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
  qdrant_data:
    driver: local
  rustfs_data:
    driver: local

configs:
  qdrant_config:
    content: |
      log_level: INFO
