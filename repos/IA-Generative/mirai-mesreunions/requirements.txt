flask==3.1.0
flask-socketio==5.4.1
authlib==1.4.1
cryptography==44.0.0
requests==2.32.3
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
asyncpg==0.30.0
boto3==1.36.5
pika==1.3.2
qrcode[pil]==8.0
clamd==1.0.2
gunicorn==23.0.0
eventlet==0.37.0
# Pure-Python document generation for Feature 3 user-facing transcript downloads
# (.docx via python-docx, .odt via odfpy). No LibreOffice binary needed.
python-docx==1.1.2
odfpy==1.4.1
# Document text extraction for the meeting-prep route (reads user-supplied
# prep documents from suitenumerique/drive). docx + odt + odp reuse the
# libraries above; we only add the formats not yet covered.
pypdf==5.1.0
python-pptx==1.0.2
# RRULE arithmetic for Preparation.recurrence_rule (Lot 6 — calcule
# `next_occurrence_at` à partir d'un dict {freq, interval, byweekday,
# byhour, byminute, until} et d'une date de référence).
python-dateutil==2.9.0.post0
# MCP SDK pour exposer video-ingest (et autres connecteurs) via le
# transport streamable-http. Consommé par Open WebUI et agents.
mcp>=1.2.0
