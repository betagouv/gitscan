# fastapi 0.136.3 tire starlette 1.2.1, qui corrige CVE-2025-62727 (ReDoS Range
# FileResponse), PYSEC-2026-161 (Host header) et CVE-2025-54121 (multipart DoS).
fastapi==0.136.3
starlette>=1.0.1  # plancher de sécurité explicite (dép. transitive de fastapi)
# /!\ starlette >=1.0 a SUPPRIMÉ l'ancienne signature TemplateResponse(name, context).
# Tous les rendus doivent utiliser TemplateResponse(request, name, context).
uvicorn[standard]==0.49.0
pydantic-settings==2.14.1
boto3==1.43.24
psycopg2-binary>=2.9.10,<3.0
alembic>=1.13.0,<2.0
PyJWT[crypto]>=2.9.0,<3.0
jinja2>=3.1.4,<4.0
python-multipart>=0.0.9,<0.1
aiofiles>=23.0.0,<25.0
slowapi>=0.1.9,<0.2
httpx==0.28.1
prometheus-client>=0.20,<1.0  # métriques proxy LLM (histogrammes latence /llm/v1)
defusedxml>=0.7.1,<0.8  # parsing XML durci (anti-XXE) du description.xml des OXT
tenacity==9.1.4  # retries bornés (backoff exponentiel + jitter) sur les appels réseau transitoires
# Observabilité (OTel) — désactivée par défaut (OTEL_EXPORTER_OTLP_ENDPOINT absent = no-op),
# donc sans impact en environnement air-gapped. Versions pinnées ensemble (même release train).
opentelemetry-api==1.37.0
opentelemetry-sdk==1.37.0
opentelemetry-exporter-otlp-proto-http==1.37.0
opentelemetry-instrumentation-fastapi==0.58b0
opentelemetry-instrumentation-httpx==0.58b0
opentelemetry-instrumentation-psycopg2==0.58b0
